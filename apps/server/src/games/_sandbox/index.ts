/**
 * Framework de mesa livre (sandbox). Reutilizavel por qualquer jogo sem regras:
 * forneca um catalogo + layout inicial em `setup`, use estes moves/view.
 */
export { makeSandboxMoves, SANDBOX_MOVE_NAMES } from './sandbox.reducers';
export { sandboxPlayerView } from './sandbox.view';
